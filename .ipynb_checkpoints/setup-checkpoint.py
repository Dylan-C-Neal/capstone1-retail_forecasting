from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Use predictive modeling to predict department-wide sales for each store in a retail chain',
    author='Dylan Neal',
    license='',
)
